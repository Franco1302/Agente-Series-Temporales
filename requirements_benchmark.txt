# Dependencias exclusivas del benchmark MCP.
# Instalar en un entorno virtual SEPARADO (ver README_MCP_SETUP.md).
# NO modifica ni reemplaza requirements.txt del proyecto principal.

ollama>=0.3.0
tabulate>=0.9.0
